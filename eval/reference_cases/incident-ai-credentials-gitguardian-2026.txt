===SOURCE===
非真實蒐集樣本。依已公開發表的統計報告改寫,非逐字複製任何真實程式碼:
GitGuardian《State of Secrets Sprawl 2026》報告指出,AI服務憑證(Hugging Face token、
Azure OpenAI key等)在2025年成長81%,是外洩密鑰類別中成長最快的一類;同份報告也指出
AI輔助的commit洩漏密鑰的比例(3.2%)是整體基準(1.5%)的兩倍以上。本案例依這個統計
描述的常見模式(AI生成程式碼時把服務金鑰直接寫死在設定檔或程式碼中)改寫成一段
簡化示範,不是任何真實repo或commit的原始碼。

===CATEGORY===
AI服務金鑰(Hugging Face token)寫死在程式碼中(對照 GitGuardian 2026 報告提及的高成長外洩類別改寫)

===EXPECTED===
custom_secret_var

===CODE===
import { HfInference } from '@huggingface/inference';

const HF_TOKEN = "hf_AbCdEfGhIjKlMnOpQrStUvWxYzABCDEFGH";
const hf = new HfInference(HF_TOKEN);

export async function classifyText(text) {
  return hf.textClassification({ model: 'distilbert-base-uncased', inputs: text });
}
