===SOURCE===
真實蒐集,來自 SecurityEval 資料集(Siddiq & Santos, MSR4P&S'22, MIT授權, github.com/s2e-lab/SecurityEval)。
案例ID: CWE-759_mitre_1,逐字取自資料集的 Insecure_code 欄位。

修正紀錄: 原本 M3 不涵蓋 hashlib.new('md5') 再 .update(Password) 的兩段式寫法,
曾以 known-gap 案例留在 reference_cases/。新增規則後已能命中,移回 cases/ 計入統計。

===CATEGORY===
hashlib.new('md5')兩段式建構儲存密碼(CWE-759)

===EXPECTED===
weak_hash

===CODE===
import hashlib


def storePassword(userName, Password):
    hasher = hashlib.new('md5')
    hasher.update(Password)
    hashed_password = hasher.digest()
    return updateUserLogin(userName, hashed_password)
