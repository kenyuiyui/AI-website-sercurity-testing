===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 正確使用JSON.parse處理資料,不是eval

===CATEGORY===
正確使用JSON.parse處理資料,不是eval

===EXPECTED===
NONE

===CODE===
function parseUserData(jsonString) {
  return JSON.parse(jsonString);
}
