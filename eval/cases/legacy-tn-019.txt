===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: Django connection.cursor()搭配%s佔位符的參數化查詢(官方推薦的安全寫法)

===CATEGORY===
Django connection.cursor()搭配%s佔位符的參數化查詢(官方推薦的安全寫法)

===EXPECTED===
NONE

===CODE===
from django.db import connection

def get_book_price_above(threshold):
    with connection.cursor() as cursor:
        cursor.execute("SELECT * FROM library_book WHERE price > %s", [threshold])
        return cursor.fetchall()
