===SOURCE===
真實蒐集,來自 SecurityEval 資料集(Siddiq & Santos, MSR4P&S'22, MIT授權, github.com/s2e-lab/SecurityEval)。
案例ID: CWE-089_codeql_1,逐字取自資料集的 Insecure_code 欄位。

修正紀錄: 原本 M9 不涵蓋 Python % 格式化("...%s..." % variable),曾以 known-gap
案例留在 reference_cases/。新增 PERCENT_FORMAT_PATTERN 後已能命中,移回 cases/ 計入統計。

===CATEGORY===
SQL查詢使用%s字串格式化組成(CWE-089 SQL Injection)

===EXPECTED===
possible_sql_injection

===CODE===
import mysql.connector

def get_connection():
    return mysql.connector.connect(host="localhost", user="", passwd="")

def show_user(username):
    cursor = get_connection().cursor()
    cursor.execute("SELECT * FROM users WHERE username = '%s'" % username)
    user = cursor.fetchone()
    return user
