===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 金鑰從環境變數讀取(正確做法),不是寫死的明文字串

===CATEGORY===
金鑰從環境變數讀取(正確做法),不是寫死的明文字串

===EXPECTED===
NONE

===CODE===
const OPENAI_KEY = process.env.OPENAI_API_KEY;
const client = new OpenAI({ apiKey: OPENAI_KEY });
