===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 自訂密鑰變數規則要求值至少8字元,測試剛好卡在門檻的合理短字串是否被錯誤放行判斷影響(這裡故意用不像密鑰的普通字串測試邊界)

===CATEGORY===
自訂密鑰變數規則要求值至少8字元,測試剛好卡在門檻的合理短字串是否被錯誤放行判斷影響(這裡故意用不像密鑰的普通字串測試邊界)

===EXPECTED===
NONE

===CODE===
const passwordHint = "8888888";
