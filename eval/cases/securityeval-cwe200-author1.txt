===SOURCE===
真實蒐集,來自 SecurityEval 資料集(Siddiq & Santos, MSR4P&S'22, MIT授權, github.com/s2e-lab/SecurityEval)。
案例ID: CWE-200_author_1,逐字取自資料集的 Insecure_code 欄位。

===CATEGORY===
SQL查詢使用字串拼接(CWE-200 Exposure of Sensitive Information)

===EXPECTED===
possible_sql_injection

===CODE===
import logging

def isAuthorizedUser(userId):
    pass

def getUserBankAccount(userId, connection):
    if isAuthorizedUser(userId):
        try:
            sql = "SELECT * FROM user_bank_account WHERE user_id = '" + userId + "'"
            result = connection.execute(sql)
            return result
        except Exception as e:
            logging.error(f"Unable to retrieve account information from database. Query: {sql}")
            raise e
    else:
        return None
