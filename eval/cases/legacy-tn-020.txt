===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: Flask官方推薦寫法:os.environ.get()讀取SECRET_KEY,無明文fallback(依python-dotenv官方教學查證)

===CATEGORY===
Flask官方推薦寫法:os.environ.get()讀取SECRET_KEY,無明文fallback(依python-dotenv官方教學查證)

===EXPECTED===
NONE

===CODE===
from dotenv import load_dotenv
import os

load_dotenv()
SECRET_KEY = os.environ.get('SECRET_KEY')
DATABASE_URL = os.environ.get('DATABASE_URL')
