===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: Python用os.environ[...]的强制讀取寫法(缺少值會直接丟例外,這是官方文件建議的"fail fast"作法,不是明文金鑰)

===CATEGORY===
Python用os.environ[...]的强制讀取寫法(缺少值會直接丟例外,這是官方文件建議的"fail fast"作法,不是明文金鑰)

===EXPECTED===
NONE

===CODE===
from os import environ
SECRET_KEY = environ["SECRET_KEY"]
SQLALCHEMY_DATABASE_URI = environ["DATABASE_URL"]
