===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 正確使用bcrypt雜湊密碼,不是MD5/SHA1

===CATEGORY===
正確使用bcrypt雜湊密碼,不是MD5/SHA1

===EXPECTED===
NONE

===CODE===
const bcrypt = require('bcrypt');
async function hashPassword(password) {
  return await bcrypt.hash(password, 12);
}
