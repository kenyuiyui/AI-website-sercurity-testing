===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 環境變數讀取的fallback是合理的預設值(localhost),不是密鑰

===CATEGORY===
環境變數讀取的fallback是合理的預設值(localhost),不是密鑰

===EXPECTED===
NONE

===CODE===
const dbHost = process.env.DB_HOST || "localhost";
const dbPort = process.env.DB_PORT || "5432";
