===SOURCE===
既有誤判測試案例集(改版前),依官方文件(Django/Flask等)查證撰寫的安全/正確寫法範例。原始描述: 變數名含token但值是佔位字樣,不是真的密鑰

===CATEGORY===
變數名含token但值是佔位字樣,不是真的密鑰

===EXPECTED===
NONE

===CODE===
const apiToken = "your-token-here";
const secretKey = "<REPLACE_ME>";
